{
  "name": "@memograph/cli",
  "version": "0.1.0",
  "description": "rememo vault를 파일에서 즉석 파싱해 그래프 관계를 조회하는 헤드리스 CLI (LLM 에이전트용)",
  "private": true,
  "bin": {
    "rememo": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@memograph/core": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
